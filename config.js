const config = {
    appConfig: {
        host: 'http://localhost/',
        port: 5000
    },
    dbConfig: {
        port: 27017,
        host: 'localhost',
        dbName: 'photoBook-db'
    }
}

module.exports = config;